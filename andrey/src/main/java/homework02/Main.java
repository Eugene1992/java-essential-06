package homework02;

/**
 * @TODO Javadoc
 * Created by User on 20.08.16.
 */
class Main {
    public static void main(String[] args) {
        ShapeExec exec = new ShapeExec();
        exec.start();


        /*Shape [] arrShapes = {smallSquare, bigSquare, trapez, rombik, kolo, orbit, tri};
        for (Shape shape: arrShapes) {
            System.out.printf("The area of %s is : %10.2f \n",shape.getName(),shape.getArea());
        }
*/
    }
}
